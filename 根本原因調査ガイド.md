# 🔍 CoinSystem 根本原因調査ガイド

## 🚨 発見された重要な問題

### Unity Editor ログから判明した問題
```
TLS Allocator ALLOC_TEMP_TLS, underlying allocator ALLOC_TEMP_MAIN has unfreed allocations, size 62
Internal: Stack allocator ALLOC_TEMP_MAIN has unfreed allocations, size 62
To Debug, run app with -diag-temp-memory-leak-validation cmd line argument.
```

**これは Unity 内部でのメモリリークが繰り返し発生していることを示しています！**

---

## 📋 根本原因調査の手順

### STEP 1: 詳細診断モードでの起動

#### 方法1: バッチファイル使用（推奨）
```batch
# プロジェクトフォルダの Unity_Diagnostics_Launcher.bat を実行
cd "c:\Users\kumod\My project"
Unity_Diagnostics_Launcher.bat

# 選択肢から「2」または「3」を選択
```

#### 方法2: 手動コマンド
```powershell
# 詳細メモリ診断モード
& 'C:\Program Files\Unity\Hub\Editor\2022.3.22f1\Editor\Unity.exe' -projectPath 'c:\Users\kumod\My project' -diag-temp-memory-leak-validation

# 完全デバッグモード（さらに詳細）
& 'C:\Program Files\Unity\Hub\Editor\2022.3.22f1\Editor\Unity.exe' -projectPath 'c:\Users\kumod\My project' -diag-temp-memory-leak-validation -enableCodeCoverage -logFile "C:\Users\kumod\Desktop\Unity_Deep_Debug.log"
```

### STEP 2: DebugLogger の使用

**Unity実行中に以下を確認:**

1. **画面右下の「Deep Debug Logger」パネル**
   - エラーキャプチャ数を監視
   - 「Trigger Analysis」で手動解析実行

2. **コンソールログの詳細確認**
   - `[DEEP_DEBUG]` タグ付きログを注目
   - スタックトレースの `🎯 CoinSystem関連` 部分を確認

3. **保存されたログファイル**
   - 場所: `%USERPROFILE%\AppData\LocalLow\DefaultCompany\My project\CoinSystem_DebugLog.txt`

### STEP 3: 特定の操作での問題再現

**以下の順序で操作して、どこでエラーが発生するかを特定:**

```
1. Unity起動 → ログ確認
2. Space キー（コイン排出） → エラー発生タイミング記録
3. R キー（リセット） → メモリリークの有無確認
4. T キー（100枚テスト） → 大量処理時のエラーパターン
5. 各操作後に「Trigger Analysis」実行
```

---

## 🎯 根本原因の特定方法

### AudioListener重複問題の調査

**DebugLoggerが自動でキャプチャする情報:**
- AudioListenerが作られる正確なタイミング
- どのGameObjectに付いているか
- 呼び出し元のスタックトレース
- 親子関係の詳細

**調査ポイント:**
```
🔍 確認事項:
1. coinPrefab に AudioListener が付いているか？
2. Instantiate時に自動で AudioListener が追加されるか？
3. AudioSource作成時に AudioListener も作られるか？
4. 子オブジェクトに隠れた AudioListener があるか？
```

### メモリリーク問題の調査

**Unityが検出したリーク:**
- `ALLOC_TEMP_MAIN` アロケーター
- 62バイトの未解放メモリ
- 繰り返し発生パターン

**調査ポイント:**
```
🔍 確認事項:
1. WaitForSeconds の重複作成
2. コルーチンの停止漏れ
3. オブジェクトプールの返却漏れ
4. Debug.Log の大量出力
5. AudioSource の重複作成
```

---

## 📊 ログ解析のポイント

### 重要なログメッセージの見方

#### AudioListener関連
```
🚨 AudioListener ERROR CAPTURED:
  GameObject: [オブジェクト名]
  Scene Path: [階層パス]
📍 CALL STACK ANALYSIS:
  🎯 [CoinSystem関連のメソッド] ← 原因特定の鍵
```

#### メモリ関連
```
🧠 MEMORY ERROR CAPTURED:
📊 Object Count Analysis:
  [オブジェクト名]: [数] instances ⚠ ← 異常な数のオブジェクト
```

#### スタックトレース分析
```
  🎯 CoinSystem.CoinDispenser.CreateCoinObject ← CoinSystem関連
  🔊 UnityEngine.AudioSource.Play ← Audio関連
  🔧 UnityEngine.Object.Instantiate ← Unity内部
```

---

## 🛠 具体的な修正アプローチ

### 発見された問題に基づく修正戦略

#### 1. AudioListener重複の根本解決
```csharp
// coinPrefab の事前チェック・修正
// CreateCoinObject での完全除去
// 子オブジェクトの徹底検査
```

#### 2. メモリリークの根本解決
```csharp
// WaitForSeconds キャッシュシステム
// コルーチン管理の厳密化
// オブジェクトプール完全性確保
```

#### 3. 診断情報の活用
```csharp
// Unity診断モードの常時利用
// DebugLogger の詳細情報で原因特定
// 修正効果の定量的測定
```

---

## ⚡ クイックトラブルシューティング

### 問題発生時の即座対応

#### AudioListener大量メッセージ
1. 画面で「EMERGENCY FIX NOW!」クリック
2. コンソールで原因オブジェクトを特定
3. DebugLogger で詳細スタックトレース確認

#### メモリ使用量急増
1. 「Trigger Analysis」で詳細調査
2. Object Count Analysis で原因オブジェクト特定
3. 「O」キーで自動最適化実行

#### システム応答なし
1. F キーで強制リセット
2. Unity診断モードで再起動
3. ログファイルで直前の操作を分析

---

## 📈 改善効果の測定

### Before/After比較項目

```
測定項目:
✓ AudioListener数: 1個で安定
✓ メモリ使用量: 100MB未満維持
✓ GameObject数: 1000個未満
✓ エラーメッセージ: 0件/分
✓ FPS: 60fps以上
✓ Unity診断警告: 0件
```

**この詳細調査システムにより、毎秒500メッセージ問題とメモリリークの正確な原因と発生タイミングが特定できます！**